version: '3.8'

services:
  frontend:
    ports:
      - "${FRONTEND_PORT_HTTPS:-443}:443"
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt:ro
      - ./wms-frontend/nginx.prod.conf:/etc/nginx/conf.d/default.conf:ro
